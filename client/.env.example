# GraphQL request method for testing (xhr | fetch | fetch-proxy)
# Default: xhr (most compatible)
VITE_GRAPHQL_METHOD=fetch

# Optional: override GraphQL endpoint (must be absolute URL for fetch/xhr)
# VITE_GRAPHQL_URL=http://localhost:4000/graphql
